<form method="post" action="{{ path('app_planning_delete', {'id': planning.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cette réservation?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planning.id) }}">
    <button  class="btn btn btn-danger ">Annuler</button>
   
</form>
