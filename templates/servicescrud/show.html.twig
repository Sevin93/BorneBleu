{% extends 'base.html.twig' %}

{% block title %}Services{% endblock %}

{% block body %}
    <h1>Services</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ service.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ service.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_servicescrud_index') }}">back to list</a>

    <a href="{{ path('app_servicescrud_edit', {'id': service.id}) }}">edit</a>

    {{ include('servicescrud/_delete_form.html.twig') }}
{% endblock %}
