{% extends 'base.html.twig' %}

{% block title %}Bornes{% endblock %}

{% block body %}
    <h1>Bornes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ borne.id }}</td>
            </tr>
            <tr>
                <th>Numero_borne</th>
                <td>{{ borne.numeroBorne }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bornes_index') }}">back to list</a>

    <a href="{{ path('app_bornes_edit', {'id': borne.id}) }}">edit</a>

    {{ include('bornes/_delete_form.html.twig') }}
{% endblock %}
