<div class="container col-6 d-grid gap-4 mb-5">
    <div class="row">         
        <div class="card px-5 py-4">
            <div class="card-body">
                <div class="mb-3 row">
{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="d-grid gap-2 col-3 mx-auto">
    <button class="btn btn-warning">{{ button_label|default('Enregistrer') }}</button>
</div>
{{ form_end(form) }}
</div>
